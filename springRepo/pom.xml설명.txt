pom.xml

Project Object Model

<project>

	xmlns="..."									// xmln(ame)s(pace)
	xmlns:xsi="..."				
	xsi:schemaLocation="..."	
	
	// 프로젝트 정보
	<modelVersion> </modelVersion>
	<grouId> </grouId>
	<artifactId> 앱이름(최상단 경로) </artifactId>
	<name> 프로젝트명 </name>
	<packaging> war <packaging>					// war : web 어플리케이션을 위해 만들어진 파일
	<version> 1.0.0-BUILD-SNAPSHOT </version>	//
	
	<properties>								// 변수들
		<java-version> 1.6 <java-version>
		<org.springframework-version> 3.1.1 RELEASE </org.springframework-version>
		<org.aspectj-version>  </org.aspectj-version>
		<org.slf4j-version>  </org.slf4j-version>
	</properties>
	
	<dependencies>								// Library 관련
	
		<dependency>							// 자동으로 Library 다운됨.
			<grouId> .. </grouId>
			<artifactId> .. </artifactId>
			<version> {org.springframework-version} </version>	// properties 의 변수
			<version> 3.1.1 RELEASE </version>													// 그냥 적어도 됨.
		</dependency>
		
		<dependency>
		
		</dependency>
		
		<dependency>
		
		</dependency>
		
	</dependencies>
	
	<build>
		<plugins>
			<plugin>
				.. 
			</plugin>
		</plugins>
	</build>
	
</project>